"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var withProperties_exports = {};
__export(withProperties_exports, {
  withProperties: () => withProperties
});
module.exports = __toCommonJS(withProperties_exports);
var import_schema_utils = require("../schema-utils");
var import_getObjectLikeUtils = require("./getObjectLikeUtils");
var import_types = require("./types");
function withProperties(objectLike, properties) {
  const objectSchema = {
    ...import_types.OBJECT_LIKE_BRAND,
    parse: (raw, opts) => {
      const parsedObject = objectLike.parse(raw, opts);
      const additionalProperties = Object.entries(properties).reduce((processed, [key, value]) => {
        return {
          ...processed,
          [key]: typeof value === "function" ? value(parsedObject) : value
        };
      }, {});
      return {
        ...parsedObject,
        ...additionalProperties
      };
    },
    json: (parsed, opts) => {
      const addedPropertyKeys = new Set(Object.keys(properties));
      const parsedWithoutAddedProperties = Object.entries(parsed).reduce((filtered, [key, value]) => {
        if (!addedPropertyKeys.has(key)) {
          filtered[key] = value;
        }
        return filtered;
      }, {});
      return objectLike.json(parsedWithoutAddedProperties, opts);
    }
  };
  return {
    ...objectSchema,
    ...(0, import_schema_utils.getSchemaUtils)(objectSchema),
    ...(0, import_getObjectLikeUtils.getObjectLikeUtils)(objectSchema)
  };
}
//# sourceMappingURL=withProperties.js.map
