"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var getSchemaUtils_exports = {};
__export(getSchemaUtils_exports, {
  getSchemaUtils: () => getSchemaUtils,
  optional: () => optional,
  transform: () => transform
});
module.exports = __toCommonJS(getSchemaUtils_exports);
var import_types = require("./types");
function getSchemaUtils(schema) {
  return {
    optional: () => optional(schema),
    transform: (transformer) => transform(schema, transformer)
  };
}
function optional(schema) {
  const baseSchema = {
    parse: (raw, opts) => raw != null ? schema.parse(raw, opts) : void 0,
    json: (parsed, opts) => parsed != null ? schema.json(parsed, opts) : void 0
  };
  return {
    ...import_types.OPTIONAL_BRAND,
    ...baseSchema,
    ...getSchemaUtils(baseSchema)
  };
}
function transform(schema, transformer) {
  const baseSchema = {
    parse: (raw, opts) => {
      const postTransformParsed = schema.parse(raw, opts);
      return transformer.parse(postTransformParsed, opts);
    },
    json: (parsed, opts) => {
      const preTransformParsed = transformer.json(parsed, opts);
      return schema.json(preTransformParsed, opts);
    }
  };
  return {
    ...baseSchema,
    ...getSchemaUtils(baseSchema)
  };
}
//# sourceMappingURL=getSchemaUtils.js.map
