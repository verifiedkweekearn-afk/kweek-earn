"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var BasicAuth_exports = {};
__export(BasicAuth_exports, {
  BasicAuth: () => BasicAuth
});
module.exports = __toCommonJS(BasicAuth_exports);
var import_basic_auth = require("basic-auth");
var import_js_base64 = require("js-base64");
const BasicAuth = {
  toAuthorizationHeader: (basicAuth) => {
    if (basicAuth == null) {
      return void 0;
    }
    const token = import_js_base64.Base64.encode(`${basicAuth.username}:${basicAuth.password}`);
    return `Basic ${token}`;
  },
  fromAuthorizationHeader: (header) => {
    const parsed = (0, import_basic_auth.parse)(header);
    if (parsed == null) {
      throw new Error("Invalid basic auth");
    }
    return {
      username: parsed.name,
      password: parsed.pass
    };
  }
};
//# sourceMappingURL=BasicAuth.js.map
